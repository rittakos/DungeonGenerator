#pragma once

#include "vector.hpp"

namespace Math
{
	

	template<class Type>
	Vec<3, Type> rotate(const Vec<3, Type>& v, Angle angle, Vec<3, Type> center = Vec<3, Type>())
	{
		return (v1 - v2).length();
	}
}